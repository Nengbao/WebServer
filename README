A simple HTTP/HTTPS web server supporting GET and HEAD requests.
- Support persistent connection to conform to HTTP 1.1 protocol
- Support different file types and different response status codes (200, 404 etc.)
- Support concurrent connections through multi-threading

Compile:
    open a terminal
    cd to the dir with makefile and java files are located
    issue "make" command

Run:
    open a terminal
    cd to the dir with makefile and java files are located
    issue "make run" command (the used port are --serverPort=10000 --sslServerPort=10001)
    or java Server --serverPort=portNum1 --sslServerPort=portNum2
